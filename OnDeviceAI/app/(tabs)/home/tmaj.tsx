import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

export default function TellMeAJokeScreen() {
    return (
        <ThemedView>
            <ThemedText>Tell Me a Joke</ThemedText>
        </ThemedView>
    );
}